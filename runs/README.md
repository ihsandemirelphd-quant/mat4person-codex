# Run artifacts

Each run receives an immutable identifier and separate candidate, verified,
rejected, and report outputs. Generated run contents remain ignored until a
reviewed freeze explicitly promotes sanitized artifacts into version control.
